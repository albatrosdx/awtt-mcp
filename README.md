# AWTT MCP Handson — Hosted MCP Server でデータ整備と可視化

「荒れた」Salesforce 組織を再現するテストデータと、AI に**安全に**一括更新させるための
カスタム MCP ツール（プレビュー → 承認 → 適用 → ロールバック）をまとめた SFDX プロジェクトです。

- 何度でも同じデータを再生成できる（乱数シード固定・シードキーで上書き）→ 参加者全員が同じ状態から開始
- 標準の Hosted MCP Server（`sobject-reads` など）だけでも調査・分析シナリオが成立する
- 更新系は Apex アクションとして実装し、**カスタム MCP サーバ**として公開 → ガードレールと監査ログ付き

## 含まれるもの

| 種別 | 名前 | 用途 |
| --- | --- | --- |
| アプリ | `HO MCPハンズオン` | 準備画面・変更ログ・各オブジェクトへのナビゲーション |
| LWC / タブ | `ハンズオン準備` (`hoWorkshopSetup`) | データ生成 / 初期状態に戻す / 全削除 / ベースラインとの差分表示 |
| カスタム項目 | `HO_Seed_Key__c`, `HO_Workshop_Data__c` ほか | テストデータの識別。`HO_都道府県`・`HO_法人番号`・`HO_失注理由`・`HO_担当営業` |
| カスタムオブジェクト | `HO_Change_Log__c` | MCP ツール経由の変更ログ（項目単位の変更前後・理由・バッチID・状態） |
| | `HO_Baseline__c` | 生成直後のスナップショット（どの経路で変更されても差分を検出） |
| カスタムメタデータ | `HO_Mcp_Guardrail__mdt` | AI に更新させてよいオブジェクト・項目・1バッチ件数・対象データ範囲 |
| Apex アクション (MCP ツール) | `HO: データ品質レポート（参照のみ）` | 空欄・表記ゆれ分布・重複候補・命名規則違反・商談の不整合 |
| | `HO: ベースラインとの差分（参照のみ）` | 更新前後の差分チェック |
| | `HO: 一括更新プレビュー（変更なし）` | 変更案の検証とバッチID発行 |
| | `HO: 一括更新の適用` | 承認済みバッチの適用（楽観ロック・60分で失効） |
| | `HO: 一括更新のロールバック` | 適用済みバッチの取り消し |
| | `HO: 取引先の重複統合` | 既定は dryRun（影響範囲のみ）。実行は明示指定時だけ |
| 権限セット | `HO_Workshop_Admin` | 講師・メンター（準備画面 + 全ツール） |
| | `HO_MCP_ReadOnly` | 参加者・前半（参照系ツールのみ） |
| | `HO_MCP_Editor` | 参加者・後半（更新系ツール。削除は取引先統合のための取引先のみ） |
| テスト | `HO_WorkshopTest` | 11メソッド。最低限のアクセスプロファイル + `HO_MCP_ReadOnly` のユーザーで「分析はできるが更新は拒否される」ことも検証 |

## 生成されるデータ（どの組織でも同じ内容）

| オブジェクト | 件数 | 仕込んである「荒れ」 |
| --- | --- | --- |
| 商品 (Product2) | 34 | 商品コード規則違反（小文字・`_`・桁数・別体系・未設定）、ファミリーのゆれ（Software/ソフトウェア/SW）、半角カナ・全角英数、重複商品 |
| 取引先 (Account) | 56 (40社 + 重複16) | `(株)`/`㈱`/後株、半角カナ、全角スペース、カタカナ表記の重複、業種のゆれ（Manufacturing/製造業/メーカー）、都道府県のゆれ（東京都/東京/Tokyo/大坂府）、電話番号の書式ばらばら、空欄 |
| 取引先責任者 (Contact) | 136 | メール空欄・大文字・ドメインのタイプミス、役職/部署のゆれ、重複取引先にまたがる同一人物 |
| リード (Lead) | 120 | 既存取引先の表記ゆれ会社名、リードソースのゆれ（Web/web/Webサイト）、重複リード、空欄 |
| 商談 (Opportunity) | 260 | 直近12か月＋今後6か月。後半フェーズなのに金額なし、失注理由の未選択（詳細メモから推定可能）、完了予定日超過の放置案件。担当営業6名ごとに受注率・単価・活動量・失注傾向に差をつけてある |
| 活動 (Task) | 1,910 | 担当営業・商談ごとの電話/メール/訪問 |

「安易に直してはいけない」判断ポイントも入れてあります（例: `株式会社北斗建設` と `北斗建設株式会社` は法人番号が異なる別会社、`株式会社はるか教育 大阪支店` は支店）。
詳細と答えは [docs/facilitator-guide.md](docs/facilitator-guide.md) を参照。

## セットアップ

### A. 自分の開発組織に直接デプロイ（講師・メンター向け）

```powershell
sf org login web --alias handson01
./scripts/setup-org.ps1 -TargetOrg handson01
```

デプロイ → `HO_Workshop_Admin` 付与 → データ生成まで一括で行います。
データだけ作り直す場合は `-SkipDeploy`、または画面の「テストデータを生成 / 初期状態に戻す」を押します。

### B. パッケージで配布（参加者向け）

パッケージ `AWTT MCP Handson` は Dev Hub `prod` に作成済みです（`sfdx-project.json` の `packageAliases`）。
新しいバージョンを作るときは次を実行します（Dev Hub で「ロック解除パッケージと第2世代管理パッケージ」が有効であること）。

```powershell
./scripts/create-package.ps1 -DevHub prod             # 作成 + 昇格（Developer Edition などに入れる場合）
./scripts/create-package.ps1 -DevHub prod -NoPromote  # スクラッチ組織での検証用（ベータ版）
```

最新バージョン: `0.1.0.2`（`04tgL000000TM1xQAG`、ベータ版・未昇格のためスクラッチ組織と Sandbox にのみインストール可能）

参加者の作業は3つだけです。

1. 表示されたインストールURLからインストール（「管理者のみ」で可）
2. 自分に権限セット `HO_Workshop_Admin` を割り当て（設定 → ユーザー → 権限セットの割り当て）
3. アプリ「HO MCPハンズオン」→「ハンズオン準備」→「テストデータを生成 / 初期状態に戻す」

> ロック解除パッケージはインストール後スクリプトに対応していないため（バージョン作成時にエラーになることを確認済み）、
> 権限付与とデータ生成は自動化せず、上記の手順で行います。

### C. データ操作だけ行う

```powershell
sf apex run --file scripts/apex/generate.apex        --target-org handson01  # 生成 / 初期化
sf apex run --file scripts/apex/delete.apex          --target-org handson01  # 全削除
sf apex run --file scripts/apex/quality-report.apex  --target-org handson01  # 答え合わせ用レポート
```

## MCP の構成

[docs/mcp-architecture.md](docs/mcp-architecture.md) を参照（外部クライアントアプリケーションの設定、使用する MCP サーバ、
カスタムサーバへのツール登録、権限設計、一括更新の流れ）。

## 検証状況（2026-09-17）

| 項目 | 環境 | 結果 |
| --- | --- | --- |
| `setup-org.ps1` によるデプロイ（RunLocalTests） | スクラッチ組織 | 成功（テスト11件） |
| データ件数・答え合わせ資料との突き合わせ | スクラッチ組織 | 件数一致。重複・命名違反・担当営業ごとの傾向を確認 |
| プレビュー → 適用 → 差分 → ロールバック、統合の影響確認、再生成の冪等性 | スクラッチ組織（Apex から各ツールを呼び出し） | 成功 |
| パッケージバージョン作成とインストール | Dev Hub `prod` → スクラッチ組織 | 成功（コードカバー率 95%） |
| 画面操作（生成・件数・差分・削除確認ダイアログ） | パッケージを入れたスクラッチ組織（Edge を自動操作） | 成功 |
| バックグラウンド生成 | スクラッチ組織 | 成功（2ジョブの連鎖） |
| Hosted MCP Server からの実際のツール呼び出し | — | 未検証（Developer Edition での提供状況の確認が必要） |

検証で見つかって修正した問題:
- ロック解除パッケージはインストール後スクリプト非対応 → 削除し、手順（権限付与 + ボタン）に変更
- Developer Edition / スクラッチ組織では Queueable の連鎖が5段まで → 7段から2段に変更
- 活動のカスタム項目は権限セットでは `Task.` / `Event.` で指定する必要がある（`Activity.` は無視される）
- 権限セットに標準項目の項目レベルセキュリティが無く、最小権限のユーザーからは商談金額なども見えなかった → 追加
- PowerShell スクリプトが Windows PowerShell 5.1 で文字化け（BOM なし UTF-8）し、CLI の警告で停止していた → 修正
